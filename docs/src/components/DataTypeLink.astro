---
import { url } from '../utils';

const { type, schema } = Astro.props;
const slug = type.replace(':', '-').replace('xs-', 'xsd-');
const href = url(`${schema}-reference/data-types/${slug}/`);
---
<a href={href}>{type}</a>
