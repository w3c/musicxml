---
import elementName from '../utils/ElementName';

const { tag, schema } = Astro.props;
const href = `/musicxml/${schema}-reference/elements/${tag}/`;
---
<a id={ tag } href={ href }>{ elementName(tag) }</a>
