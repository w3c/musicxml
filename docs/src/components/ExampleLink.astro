---
import Path from 'node:path';

const { example, schema } = Astro.props;
const href = `/musicxml/${schema}-reference/examples/${Path.parse(example.file).name}/`;
---
<a href={href}>{example.frontmatter.title}</a>
