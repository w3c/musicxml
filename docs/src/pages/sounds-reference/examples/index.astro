---
import Path from 'node:path';

import BaseLayout from '../../../layouts/Base.astro';

const examples = import.meta.glob('../../../data/examples/sounds-reference/*.md', { eager: true });

const frontmatter = {
  title: 'Sounds example documents',
}
---
<BaseLayout frontmatter={frontmatter}>
  {Object.values(examples).map(example => (
    <a href={"/musicxml/sounds-reference/examples/" + Path.parse(example.file).name + "/"}>{example.frontmatter.title}</a>
    <p/>
  ))}
</BaseLayout>
